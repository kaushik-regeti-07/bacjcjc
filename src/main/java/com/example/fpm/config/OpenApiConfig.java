package com.example.fpm.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // No OpenAPI/Swagger configuration as per stack restrictions
}
